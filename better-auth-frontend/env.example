# Base URL of the Nest backend Better Auth API (no trailing slash required).
# Must match the backend BETTER_AUTH_URL origin (e.g. both http://localhost:3001).
NEXT_PUBLIC_AUTH_BASE_URL=http://localhost:3001
